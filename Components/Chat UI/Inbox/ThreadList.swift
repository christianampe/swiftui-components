import SwiftUI

struct ThreadList {
    
}

extension ThreadList: View {
    var body: some View {
       Text("Hi")
    }
}

struct ThreadList_Previews: PreviewProvider {
    static var previews: some View {
        ThreadList()
    }
}
